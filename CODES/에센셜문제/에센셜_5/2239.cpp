#include <bits/stdc++.h>

#define fastio cin.tie(0)->sync_with_stdio(0)
#define SUDOKU_LEN 9

using namespace std;

vector<vector<int>> matrix;

int main() {
	fastio;

    matrix.resize(SUDOKU_LEN, vector<int>(SUDOKU_LEN));

    pri

    cout << "ss" << '\n';
}