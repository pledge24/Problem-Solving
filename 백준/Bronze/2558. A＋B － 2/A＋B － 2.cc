#include <bits/stdc++.h>

#define fastio cin.tie(0)->sync_with_stdio(0)

using namespace std;

int main (void){
        fastio;
        int A, B; cin >> A >> B;
    
        cout << A+B << '\n';
    
        return 0;
}