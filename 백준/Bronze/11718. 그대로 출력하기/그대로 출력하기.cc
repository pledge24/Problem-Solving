#include <bits/stdc++.h>

#define fastio cin.tie(0)->sync_with_stdio(0)

using namespace std;

int main(void){
    fastio;
    
    while(!cin.eof()){
        string str; getline(cin, str);
        cout << str << '\n';
    }

    return 0;
}